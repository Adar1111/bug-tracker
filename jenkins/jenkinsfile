pipeline {
    agent any

    stages {
        stage('Unit_sest - baclend'){
            steps {
                dir('bugtracker-backend') {
                    go test -v ./...
                }

            }
        }
    }
}
// Adding a small comment to force a new git commit