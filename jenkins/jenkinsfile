pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
                sh 'echo "hello jenkins"'
                sh 'date'
            }
        }
    }
}
// Adding a small comment to force a new git commit