pipeline {
    agent any

    stages {
        stage('Execute Unit Tests') {
            parallel {
                
                // --- Backend ---
                stage('Unit Test - Backend') {
                    agent {
                        docker {
                            image 'snakee/golang-junit:1.21'
                            reuseNode true
                        }
                    }
                    steps {
                        withEnv(['GOCACHE=/tmp/go-build-cache', 'GOMODCACHE=/tmp/go-mod-cache']) {
                            dir('bugtracker-backend') {
                                sh '''
                                set -o pipefail
                                # הרצת טסטים ויצירת דוח XML - שים לב לשם הקובץ
                                go test -v ./... 2>&1 | go-junit-report > test-results.xml
                                
                                # יצירת דוח Coverage
                                go test -coverprofile coverage.out -covermode atomic ./cmd/... ./internal/...
                                go tool cover -html coverage.out -o coverage.html

                                mkdir -p reports
                                mv coverage.html reports/
                                '''
                            }
                        }
                    }
                    post {
                        always {
                            // הנתיב המדויק לקובץ ה-XML בתוך התיקייה
                            junit 'bugtracker-backend/test-results.xml'
                            publishHTML target: [
                                reportDir: 'bugtracker-backend/reports',
                                reportFiles: 'coverage.html',
                                reportName: 'Backend Coverage Report',
                                allowMissing: true 
                            ]
                        }
                    }
                }

                // --- Frontend ---
                stage('Unit Test - Frontend') {
                    agent { 
                        docker {
                            image 'node:20-alpine'
                            reuseNode true
                        }
                    }
                    steps {
                        dir('bugtracker-frontend') {
                            sh '''
                            npm ci
                            # הרצת טסטים. jest-junit יוצר כברירת מחדל קובץ בשם junit.xml
                            CI=true npm test -- --coverage --reporters=default --reporters=jest-junit
                            
                            # אין צורך להעביר את התיקייה, פשוט נפנה אליה ב-Post
                            '''
                        }
                    }
                    post {
                        always {
                            // jest-junit יוצר קובץ בשם junit.xml בשורש תיקיית הפרויקט
                            junit 'bugtracker-frontend/junit.xml'
                            publishHTML target:[
                                // הנתיב הדיפולטיבי של Jest Coverage הוא תיקיית coverage/lcov-report
                                reportDir: 'bugtracker-frontend/coverage/lcov-report',
                                reportFiles: 'index.html',
                                reportName: 'Frontend Coverage reports',
                                allowMissing: true
                            ]
                        }
                    }
                }
            } // parallel
        } // Execute Unit Tests
    }
    post {
        always {
            cleanWs()
        }
    }
}
